import React from "react";

const style = {
  textAlign: "center",
  fontWeight: "bold",
  marginTop: "50px"
};

// function component
const Footer = props => {
  return (
    <footer style={style}>
      <p>Creaciones Grupo 8</p>
    </footer>
  );
};

export default Footer;
