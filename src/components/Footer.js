import React from "react";

const style = {
  textAlign: "center",
  fontWeight: "bold",
  marginTop: "50px"
};

// function component
const Footer = props => {
  return (
    <footer style={style}>
    </footer>
  );
};

export default Footer;
